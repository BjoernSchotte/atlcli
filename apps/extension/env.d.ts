// Bun runtime types for this workspace's test files (`bun:test`) and build
// scripts (`import.meta.dir`, `Bun.*`). Coexists with @types/node under
// skipLibCheck, same as the repo root.
/// <reference types="bun-types" />
