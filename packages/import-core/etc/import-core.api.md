## API Report: @atlcli/import-core

> Generated by `scripts/api-report.ts` from the built `dist/*.d.ts`.
> Do NOT edit by hand — regenerate with `bun scripts/api-report.ts --update`
> and have the diff reviewed (spec 009, API freeze & guards).

### Entry point `. (browser)`

```ts
// export: AdfDocument
export interface AdfDocument {
    version: 1;
    type: "doc";
    content: AdfNode[];
}

// export: AdfEncodeOptions
export interface AdfEncodeOptions {
    media?: ReadonlyMap<string, AdfMediaResolution>;
    references?: ReadonlyMap<string, string>;
}

// export: AdfMediaResolution
export interface AdfMediaResolution {
    fileId: string;
    collection: string;
}

// export: AdfNode
export interface AdfNode {
    type: string;
    attrs?: Record<string, unknown>;
    content?: AdfNode[];
    marks?: {
        type: string;
        attrs?: Record<string, unknown>;
    }[];
    text?: string;
}

// export: assessEditability
export declare function assessEditability(blocks: ImportBlock[]): EditabilityAssessment;

// export: buildImportPreview
export declare function buildImportPreview(document: ImportDocumentV2, target: ImportTarget): Promise<ImportPreview>;

// export: canonicalJson
export declare function canonicalJson(value: unknown): string;

// export: documentToAdf
export declare function documentToAdf(document: Pick<ImportProjectionInput, "blocks">, options?: AdfEncodeOptions): AdfDocument;

// export: documentToStorage
export declare function documentToStorage(document: Pick<ImportProjectionInput, "blocks" | "assets">, options?: StorageEncodeOptions): string;

// export: EDITABILITY_BUDGETS
export declare const EDITABILITY_BUDGETS: {
    readonly caution: {
        readonly adfBytes: number;
        readonly nodeCount: 5000;
        readonly tableCells: 2500;
    };
    readonly risk: {
        readonly adfBytes: number;
        readonly nodeCount: 20000;
        readonly tableCells: 10000;
    };
};

// export: EditabilityAssessment
export interface EditabilityAssessment {
    adfBytes: number;
    nodeCount: number;
    tableCells: number;
    images: number;
    level: EditabilityLevel;
    recommendation?: string;
}

// export: EditabilityLevel
export type EditabilityLevel = "ok" | "caution" | "risk";

// export: IMPORT_DOCUMENT_SCHEMA_V2
export declare const IMPORT_DOCUMENT_SCHEMA_V2: "atlcli.import-document/2";

// export: ImportAsset
export interface ImportAsset {
    id: string;
    sourceRefs?: string[];
    fileName: string;
    mediaType: string;
    bytes: Uint8Array;
}

// export: ImportBlock
export type ImportBlock = (ImportNodeIdentity & {
    type: "heading";
    level: 1 | 2 | 3 | 4 | 5 | 6;
    runs: ImportRun[];
    label?: string;
}) | (ImportNodeIdentity & {
    type: "paragraph";
    runs: ImportRun[];
}) | ImportListBlock | (ImportNodeIdentity & {
    type: "table";
    rows: ImportTableRow[];
}) | ImportImageBlock | (ImportNodeIdentity & {
    type: "blockquote";
    blocks: ImportBlock[];
}) | ImportDisclosureBlock | (ImportNodeIdentity & {
    type: "code";
    text: string;
}) | (ImportNodeIdentity & {
    type: "page-break";
    sourcePageIndex?: number;
});

// export: ImportDisclosureBlock
export interface ImportDisclosureBlock extends ImportNodeIdentity {
    type: "disclosure";
    title: string;
    blocks: ImportBlock[];
}

// export: ImportDocumentV2
export interface ImportDocumentV2 {
    schema: typeof IMPORT_DOCUMENT_SCHEMA_V2;
    sourceKind: ImportSourceKind;
    titleCandidate?: string;
    blocks: ImportBlock[];
    assets: ImportAsset[];
    issues: ImportIssue[];
}

// export: ImportImageBlock
export interface ImportImageBlock extends ImportNodeIdentity {
    type: "image";
    assetId: string;
    alt?: string;
    width?: number;
    height?: number;
    presentation?: "figure" | "page-fallback" | "region-fallback";
    captionBlockId?: string;
}

// export: ImportIssue
export interface ImportIssue {
    code: string;
    severity: ImportIssueSeverity;
    outcome: ImportOutcome;
    message: string;
    sourceRefs?: string[];
    context?: Record<string, string | number>;
    action?: string;
}

// export: ImportIssueSeverity
export type ImportIssueSeverity = "info" | "warning" | "error";

// export: ImportListBlock
export interface ImportListBlock extends ImportNodeIdentity {
    type: "list";
    ordered: boolean;
    items: ImportListItem[];
}

// export: ImportListItem
export interface ImportListItem {
    blocks: ImportBlock[];
    child?: ImportListBlock;
}

// export: ImportNodeIdentity
export interface ImportNodeIdentity {
    id: string;
    sourceRefs?: string[];
    pageBoundaryBefore?: boolean;
}

// export: ImportOutcome
export type ImportOutcome = "native" | "approximated" | "attached" | "reported" | "rejected";

// export: ImportPreview
export interface ImportPreview {
    target: ImportTarget;
    counts: Record<string, number>;
    outline: {
        level: number;
        text: string;
    }[];
    assets: ImportPreviewAsset[];
    editability: EditabilityAssessment;
    issues: ImportDocumentV2["issues"];
    adfDigest: string;
}

// export: ImportPreviewAsset
export interface ImportPreviewAsset {
    fileName: string;
    mediaType: string;
    byteLength: number;
    sha256: string;
}

// export: ImportProjectionInput
export type ImportProjectionInput = Pick<ImportDocumentV2, "blocks" | "assets" | "issues">;

// export: importReferenceKey
export declare function importReferenceKey(reference: ImportReferenceLink): string;

// export: ImportReferenceLink
export interface ImportReferenceLink {
    namespace: string;
    target: string;
    fragment?: string;
}

// export: ImportRun
export type ImportRun = {
    kind: "text";
    text: string;
    marks?: ImportRunMarks;
} | {
    kind: "hard-break";
};

// export: ImportRunMarks
export interface ImportRunMarks {
    bold?: boolean;
    italic?: boolean;
    code?: boolean;
    link?: {
        href: string;
    };
    reference?: ImportReferenceLink;
}

// export: ImportSourceKind
export type ImportSourceKind = "docx" | "pdf";

// export: ImportTableCell
export interface ImportTableCell {
    id: string;
    sourceRefs?: string[];
    header: boolean;
    rowspan?: number;
    colspan?: number;
    blocks: ImportBlock[];
}

// export: ImportTableRow
export interface ImportTableRow {
    cells: ImportTableCell[];
}

// export: ImportTarget
export interface ImportTarget {
    spaceKey: string;
    title: string;
    parentId?: string;
}

// export: renderImportPreview
export declare function renderImportPreview(preview: ImportPreview): string;

// export: resolveImportReference
export declare function resolveImportReference(reference: ImportReferenceLink, references?: ReadonlyMap<string, string>): string | undefined;

// export: StorageEncodeOptions
export interface StorageEncodeOptions {
    references?: ReadonlyMap<string, string>;
}

// export: storageTagSequence
export declare function storageTagSequence(storage: string): string[];
```

### Entry point `. (default)`

```ts
// export: AdfDocument
export interface AdfDocument {
    version: 1;
    type: "doc";
    content: AdfNode[];
}

// export: AdfEncodeOptions
export interface AdfEncodeOptions {
    media?: ReadonlyMap<string, AdfMediaResolution>;
    references?: ReadonlyMap<string, string>;
}

// export: AdfMediaResolution
export interface AdfMediaResolution {
    fileId: string;
    collection: string;
}

// export: AdfNode
export interface AdfNode {
    type: string;
    attrs?: Record<string, unknown>;
    content?: AdfNode[];
    marks?: {
        type: string;
        attrs?: Record<string, unknown>;
    }[];
    text?: string;
}

// export: assessEditability
export declare function assessEditability(blocks: ImportBlock[]): EditabilityAssessment;

// export: buildImportPreview
export declare function buildImportPreview(document: ImportDocumentV2, target: ImportTarget): Promise<ImportPreview>;

// export: canonicalJson
export declare function canonicalJson(value: unknown): string;

// export: documentToAdf
export declare function documentToAdf(document: Pick<ImportProjectionInput, "blocks">, options?: AdfEncodeOptions): AdfDocument;

// export: documentToStorage
export declare function documentToStorage(document: Pick<ImportProjectionInput, "blocks" | "assets">, options?: StorageEncodeOptions): string;

// export: EDITABILITY_BUDGETS
export declare const EDITABILITY_BUDGETS: {
    readonly caution: {
        readonly adfBytes: number;
        readonly nodeCount: 5000;
        readonly tableCells: 2500;
    };
    readonly risk: {
        readonly adfBytes: number;
        readonly nodeCount: 20000;
        readonly tableCells: 10000;
    };
};

// export: EditabilityAssessment
export interface EditabilityAssessment {
    adfBytes: number;
    nodeCount: number;
    tableCells: number;
    images: number;
    level: EditabilityLevel;
    recommendation?: string;
}

// export: EditabilityLevel
export type EditabilityLevel = "ok" | "caution" | "risk";

// export: IMPORT_DOCUMENT_SCHEMA_V2
export declare const IMPORT_DOCUMENT_SCHEMA_V2: "atlcli.import-document/2";

// export: ImportAsset
export interface ImportAsset {
    id: string;
    sourceRefs?: string[];
    fileName: string;
    mediaType: string;
    bytes: Uint8Array;
}

// export: ImportBlock
export type ImportBlock = (ImportNodeIdentity & {
    type: "heading";
    level: 1 | 2 | 3 | 4 | 5 | 6;
    runs: ImportRun[];
    label?: string;
}) | (ImportNodeIdentity & {
    type: "paragraph";
    runs: ImportRun[];
}) | ImportListBlock | (ImportNodeIdentity & {
    type: "table";
    rows: ImportTableRow[];
}) | ImportImageBlock | (ImportNodeIdentity & {
    type: "blockquote";
    blocks: ImportBlock[];
}) | ImportDisclosureBlock | (ImportNodeIdentity & {
    type: "code";
    text: string;
}) | (ImportNodeIdentity & {
    type: "page-break";
    sourcePageIndex?: number;
});

// export: ImportDisclosureBlock
export interface ImportDisclosureBlock extends ImportNodeIdentity {
    type: "disclosure";
    title: string;
    blocks: ImportBlock[];
}

// export: ImportDocumentV2
export interface ImportDocumentV2 {
    schema: typeof IMPORT_DOCUMENT_SCHEMA_V2;
    sourceKind: ImportSourceKind;
    titleCandidate?: string;
    blocks: ImportBlock[];
    assets: ImportAsset[];
    issues: ImportIssue[];
}

// export: ImportImageBlock
export interface ImportImageBlock extends ImportNodeIdentity {
    type: "image";
    assetId: string;
    alt?: string;
    width?: number;
    height?: number;
    presentation?: "figure" | "page-fallback" | "region-fallback";
    captionBlockId?: string;
}

// export: ImportIssue
export interface ImportIssue {
    code: string;
    severity: ImportIssueSeverity;
    outcome: ImportOutcome;
    message: string;
    sourceRefs?: string[];
    context?: Record<string, string | number>;
    action?: string;
}

// export: ImportIssueSeverity
export type ImportIssueSeverity = "info" | "warning" | "error";

// export: ImportListBlock
export interface ImportListBlock extends ImportNodeIdentity {
    type: "list";
    ordered: boolean;
    items: ImportListItem[];
}

// export: ImportListItem
export interface ImportListItem {
    blocks: ImportBlock[];
    child?: ImportListBlock;
}

// export: ImportNodeIdentity
export interface ImportNodeIdentity {
    id: string;
    sourceRefs?: string[];
    pageBoundaryBefore?: boolean;
}

// export: ImportOutcome
export type ImportOutcome = "native" | "approximated" | "attached" | "reported" | "rejected";

// export: ImportPreview
export interface ImportPreview {
    target: ImportTarget;
    counts: Record<string, number>;
    outline: {
        level: number;
        text: string;
    }[];
    assets: ImportPreviewAsset[];
    editability: EditabilityAssessment;
    issues: ImportDocumentV2["issues"];
    adfDigest: string;
}

// export: ImportPreviewAsset
export interface ImportPreviewAsset {
    fileName: string;
    mediaType: string;
    byteLength: number;
    sha256: string;
}

// export: ImportProjectionInput
export type ImportProjectionInput = Pick<ImportDocumentV2, "blocks" | "assets" | "issues">;

// export: importReferenceKey
export declare function importReferenceKey(reference: ImportReferenceLink): string;

// export: ImportReferenceLink
export interface ImportReferenceLink {
    namespace: string;
    target: string;
    fragment?: string;
}

// export: ImportRun
export type ImportRun = {
    kind: "text";
    text: string;
    marks?: ImportRunMarks;
} | {
    kind: "hard-break";
};

// export: ImportRunMarks
export interface ImportRunMarks {
    bold?: boolean;
    italic?: boolean;
    code?: boolean;
    link?: {
        href: string;
    };
    reference?: ImportReferenceLink;
}

// export: ImportSourceKind
export type ImportSourceKind = "docx" | "pdf";

// export: ImportTableCell
export interface ImportTableCell {
    id: string;
    sourceRefs?: string[];
    header: boolean;
    rowspan?: number;
    colspan?: number;
    blocks: ImportBlock[];
}

// export: ImportTableRow
export interface ImportTableRow {
    cells: ImportTableCell[];
}

// export: ImportTarget
export interface ImportTarget {
    spaceKey: string;
    title: string;
    parentId?: string;
}

// export: renderImportPreview
export declare function renderImportPreview(preview: ImportPreview): string;

// export: resolveImportReference
export declare function resolveImportReference(reference: ImportReferenceLink, references?: ReadonlyMap<string, string>): string | undefined;

// export: StorageEncodeOptions
export interface StorageEncodeOptions {
    references?: ReadonlyMap<string, string>;
}

// export: storageTagSequence
export declare function storageTagSequence(storage: string): string[];
```
