# `@atlcli/research`

Host-neutral contracts for Atlassian research across CLI and browser hosts.

The initial surface contains only versioned request, report, and capability
contracts. It deliberately has no filesystem, browser-extension, network,
model-provider, or sandbox runtime dependency.
