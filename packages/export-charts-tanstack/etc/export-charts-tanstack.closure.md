## API Closure Classification: @atlcli/export-charts-tanstack

> **Freeze decision (spec 009 T4.2): stays 0.x — NOT frozen.**
> STAYS 0.x: pinned TanStack Charts adapter shared by Astro, DOCX, and PDF chart projections. Keep it experimental while the upstream 0.x API and the all-shapes scene/SVG contract mature through real publication consumers.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — experimental

- exported symbols (12): RenderTanStackChartSvgOptionsV1, TANSTACK_CHART_ADAPTER_V1, TANSTACK_CHART_RENDER_BUDGET_V1, TANSTACK_CHART_SIZE_V1, TanStackChartAdapterResultV1, TanStackChartApproximationV1, TanStackChartDefinitionV1, TanStackChartRenderBudgetErrorV1, TanStackChartRenderBudgetV1, createTanStackChartDefinitionV1, createTanStackChartSceneV1, renderTanStackChartSvgV1
