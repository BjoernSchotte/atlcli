## API Closure Classification: @atlcli/diagram

> **Freeze decision (spec 009 T4.2): stays 0.x — NOT frozen.**
> STAYS 0.x (plan pre-decision): renderer-internal surface beyond renderDiagram/DiagramTheme; those two are re-exported (and frozen-by-closure) via @atlcli/docx.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — experimental

- exported symbols (6): DEFAULT_DIAGRAM_THEME, DiagramRenderResult, DiagramTheme, flattenSvgStyles, renderDiagram, warmDiagramRenderer
