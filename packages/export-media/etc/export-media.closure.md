## API Closure Classification: @atlcli/export-media

> **Freeze decision (spec 009 T4.2): stays 0.x — NOT frozen.**
> STAYS 0.x: introduced for issue 118 Phase 1 as the shared deterministic image inspection/normalization codec (extracted from @atlcli/docx, consumed by the PDF and DOCX engines). Keep it experimental until the standard/print PPI pins survive a release cycle of measured corpus results.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — experimental

- exported symbols (37): DecodedRaster, EXPORT_IMAGE_PPI_MAX, EXPORT_IMAGE_PPI_MIN, EncodeRasterTargetRequestV1, ExportImageProfile, ExportImageQualityError, ExportImageQualityV1, IMAGE_NORMALIZER_VERSION, ImageFormat, ImageInfo, MAX_RASTER_AXIS_PX, MAX_RASTER_PIXELS, NORMALIZED_JPEG_QUALITY, PRINT_PROFILE_PPI, RasterKeptReason, RasterNormalizationPlanResultV1, RasterNormalizationPlanV1, RasterNormalizeRequestV1, RasterNormalizeResultV1, RasterNormalizerPortV1, STANDARD_PROFILE_PPI, TargetSize, boundRasterTarget, boxResampleRgba, decodeImageInfo, decodeJpegRaster, decodePngRaster, encodeJpeg, encodePng, encodeRasterTargetV1, isSvg, normalizeRasterAssetV1, parseSvgSize, planRasterNormalizationV1, resolveEffectivePpi, resolveTargetSize, sha256Hex
