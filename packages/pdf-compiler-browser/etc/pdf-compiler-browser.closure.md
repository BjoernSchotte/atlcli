## API Closure Classification: @atlcli/pdf-compiler-browser

> **Freeze decision (spec 009 T4.2): 1.0.0 — FROZEN.**
> Tiny, stable surface (BrowserPdfCompiler + BrowserPdfCompilerAssets) over the sha256-pinned vendored compiler; consumed identically by CLI, extension, harness, and the spec 011 parity harness.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — stable

- exported symbols (3): BrowserPdfCompiler, BrowserPdfCompilerAssets, PDF_BROWSER_COMPILER_VERSION
- same-package closure references: 1
- reaches `@atlcli/pdf` (frozen): PdfCompileResult, PdfSourceBundle
- reachable-but-unexported gaps: none
