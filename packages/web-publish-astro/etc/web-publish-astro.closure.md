## API Closure Classification: @atlcli/web-publish-astro

> **Freeze decision (spec 009 T4.2): stays 0.x — NOT frozen.**
> STAYS 0.x: Node-only Astro structured-data loader and static integration introduced for web publishing. Keep it experimental until packed clean-project, Starlight, and real Cloud/Data Center publication artifacts prove the integration contract.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — experimental

- exported symbols (9): AtlcliAstroPublicationLoaderV1, AtlcliAstroPublishingIntegrationV1, AtlcliPublicationLoaderOptionsV1, AtlcliPublishingIntegrationOptionsV1, LoadedPublicationBundleV1, atlcliPublicationLoader, atlcliPublishingIntegration, publicationRoutePathV1, readPublicationBundlePagesV1
