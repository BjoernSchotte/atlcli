## API Closure Classification: @atlcli/plugin-api

> **Freeze decision (spec 009 T4.2): stays 0.x — NOT frozen.**
> STAYS 0.x: never API-reviewed; the plugin loader story predates the freeze work.
>
> Generated by `scripts/api-closure.ts` — regenerate with
> `bun scripts/api-closure.ts --update` and have the diff reviewed.
> Classes: **stable** (frozen v1 surface), **experimental** (exported,
> may change in minors), **internal** (non-frozen implementation subpath).

### Entry point `.` — experimental

- exported symbols (14): AtlcliPlugin, CommandContext, CommandDefinition, FlagDefinition, FlagValue, OutputOptions, PluginHooks, PluginManifest, PluginMetadata, Subcommand, defineCommand, defineFlag, definePlugin, defineSubcommand
