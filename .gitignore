.DS_Store
node_modules
dist
.env
.atlcli
.tmp
.artifacts/
/tmp/
.bun
site/
.venv/
.turbo
spec/internal/
.astro/
.playwright-cli/
opensrc/

# Reproducible PDF font cache (downloaded from pinned Adobe releases).
packages/pdf/.fonts/

# Transient prepack backup written by scripts/strip-dev-condition.ts
.package.json.prepack-backup

# Reproducible vendored patched typst.ts glue+wasm (~28 MB wasm; regenerated
# deterministically from the patched install by vendor-typst.ts — see
# packages/pdf-compiler-browser/scripts/vendor-typst.ts for why not committed).
packages/pdf-compiler-browser/vendor/

# SonarQube coverage artifact
coverage/
.scannerwork/
scripts/bench/out/
scripts/bench/history/
scripts/verapdf/out/

# HEAD-bound security attestation (spec 011) — a CI artifact, never committed:
# it describes one commit's state and would be stale the moment it landed.
security-attestation.json
# Scheduled/read-only ADF drift reports
/artifacts/adf-drift/

# Private research evaluation material. Real gold sets, run envelopes, reports,
# and provider transcripts must stay outside the repository.
/.atlcli-research-private/
*.research-gold.private.json
*.research-run.private.json
*.research-report.private.md
*.rovo-transcript.private.json

# Generated image-heavy benchmark corpus (issue #118 Phase 0) — reproduced from (seed, scale)
apps/extension/tests/pdf/memory/public/image-heavy/
