{
  "name": "docx-engine-spike",
  "version": "0.0.0",
  "private": true,
  "description": "Throwaway spike (spec 004 Task 1): compares docx-templates vs docxtemplater. NOT part of the bun workspace.",
  "type": "module",
  "scripts": {
    "build:template": "bun run src/build-template.ts",
    "build:payload": "bun run src/build-payload.ts",
    "run:docx-templates": "bun run src/run-docx-templates.ts",
    "run:docxtemplater": "bun run src/run-docxtemplater.ts",
    "run:preprocessor": "bun run src/run-preprocessor.ts",
    "run:errors": "bun run src/run-errors.ts",
    "verify": "bun run src/verify-outputs.ts",
    "all": "bun run build:template && bun run build:payload && bun run run:docx-templates && bun run run:docxtemplater && bun run run:preprocessor && bun run run:errors && bun run verify"
  },
  "dependencies": {
    "docx": "^9.5.1",
    "docx-templates": "^4.14.1",
    "docxtemplater": "^3.66.6",
    "pizzip": "^3.2.0"
  }
}
